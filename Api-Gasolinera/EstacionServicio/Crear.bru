meta {
  name: Crear
  type: http
  seq: 1
}

post {
  url: {{SERVER}}estacion-servicio
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "nombre": "Estacion 1"
  }
}
